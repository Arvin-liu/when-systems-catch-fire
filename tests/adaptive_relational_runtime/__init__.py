"""Adaptive Relational Runtime R1 — test package for commit 4.

Holds the 12 cross-domain fixtures (fixtures/), the 40-item attack matrix
(attack_matrix.json + attack_cases.py), and the pytest suites that execute
them. No license header: matches the repo tests/ convention (no SPDX on
tests). The runtime tools under tools/adaptive_relational_runtime/ carry the
BUSL-1.1 header; tests do not.
"""
