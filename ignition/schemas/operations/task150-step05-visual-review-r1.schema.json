{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.invalid/ignition/operations/task150-step05-visual-review-r1.schema.json",
  "title": "Task150 Step05 visual review evidence",
  "description": "Separates automated containment, agent visual inspection and Owner aesthetic acceptance for the bounded derived visualization.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "task_id", "step", "record_type", "status", "formal_previous_commit", "automated_review", "agent_visual_inspection", "owner_visual_acceptance", "evidence_files", "scope_freeze", "claim_ceiling"],
  "properties": {
    "schema_version": {"const": "ignition-150-step05-visual-review-r1"},
    "task_id": {"const": "IGNITION-20260901-150"},
    "step": {"const": "05"},
    "record_type": {"const": "VISUAL_REVIEW_EVIDENCE"},
    "status": {"const": "AGENT_VISUAL_INSPECTION_RECORDED_OWNER_PENDING"},
    "formal_previous_commit": {"const": "e6d29c57ea54817bdebc39f0d83e5c362e6caf46"},
    "automated_review": {
      "type": "object",
      "additionalProperties": false,
      "required": ["standalone_visual_check", "standalone_visual_review", "delta_visual_check", "delta_visual_review"],
      "properties": {
        "standalone_visual_check": {"const": "PASS"},
        "standalone_visual_review": {"const": "pending"},
        "delta_visual_check": {"const": "FAIL_UPSTREAM_WRAPPER"},
        "delta_visual_review": {"const": "pending"}
      }
    },
    "agent_visual_inspection": {
      "type": "object",
      "additionalProperties": false,
      "required": ["status", "method", "browser_observation", "checks", "notes"],
      "properties": {
        "status": {"const": "STANDALONE_PASS_WITH_LIMITS_DELTA_BLOCKED"},
        "method": {"const": "LOCAL_PNG_INSPECTION_AFTER_BROWSER_FILE_URL_POLICY_BLOCK"},
        "browser_observation": {"const": "IN_APP_BROWSER_REJECTED_LOCAL_FILE_URL; NO_POLICY_BYPASS"},
        "checks": {
          "type": "object",
          "additionalProperties": false,
          "required": ["main_path", "component_crowding", "label_occlusion", "edge_visual_signal", "dark_light_readability", "viewport_hierarchy", "delta_three_panel_readability"],
          "properties": {
            "main_path": {"const": "PASS_WITH_LIMITS"},
            "component_crowding": {"const": "PASS_WITH_LIMITS"},
            "label_occlusion": {"const": "NO_OBVIOUS_OCCLUSION_IN_STANDALONE"},
            "edge_visual_signal": {"const": "NO_OBVIOUS_FALSE_DIRECTIONAL_SIGNAL"},
            "dark_light_readability": {"const": "PASS_WITH_LIMITS_STANDALONE"},
            "viewport_hierarchy": {"const": "PASS_STANDALONE_FAIL_DELTA_WRAPPER"},
            "delta_three_panel_readability": {"const": "NOT_ACCEPTED_UPSTREAM_WRAPPER_OVERFLOW_AND_THEME_RESOLUTION"}
          }
        },
        "notes": {
          "const": [
            "The standalone light and dark captures show the authored path and boundaries without an obvious node or label collision.",
            "At 1440px width the diagram remains legible but labels are small and the composition is dense; this is a bounded experiment, not an aesthetic acceptance claim.",
            "The 2048px captures preserve hierarchy with more breathing room.",
            "The Delta capture is not accepted as a readable three-panel review: the upstream shell overflows at required small viewports and the 1440px light-labelled capture resolves to the same dark artifact as the dark capture.",
            "Semantic direction and architecture meaning remain bound to the authored source and Step03 provenance contract, not to visual impression."
          ]
        }
      }
    },
    "owner_visual_acceptance": {"const": "OWNER_VISUAL_ACCEPTANCE_PENDING"},
    "evidence_files": {
      "type": "array",
      "minItems": 12,
      "maxItems": 12,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["path", "kind", "sha256", "bytes"],
        "properties": {
          "path": {"type": "string", "pattern": "^ignition/data/operations/iterations/150/visual-evidence/"},
          "kind": {"enum": ["png", "contact_sheet_html", "machine_receipt_json"]},
          "sha256": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
          "bytes": {"type": "integer", "minimum": 1}
        }
      }
    },
    "scope_freeze": {
      "type": "object",
      "additionalProperties": false,
      "required": ["task150_scope", "agent_reach", "authenticated_channels", "installation", "live_external_invocation", "current_admission"],
      "properties": {
        "task150_scope": {"const": "ARCHIFY_ONLY"},
        "agent_reach": {"const": "NO_CHANGE"},
        "authenticated_channels": {"const": "NO_AUTHENTICATED_ADMISSION"},
        "installation": {"const": "NO_INSTALL_OR_AUTO_UPGRADE"},
        "live_external_invocation": {"const": "UNCHANGED_OPEN_OWNER_DEFERRED_NOT_RUN"},
        "current_admission": {"const": "NOT_ADMITTED"}
      }
    },
    "claim_ceiling": {"const": "Task150 Step05 records automated visual evidence and a bounded agent inspection of standalone screenshots while retaining the upstream Delta-shell blocker and Owner visual acceptance pending. It does not establish perceptual quality, Owner acceptance, Current integration, a default renderer, architecture truth, production readiness, authenticated access, external truth or live external completion."}
  }
}
