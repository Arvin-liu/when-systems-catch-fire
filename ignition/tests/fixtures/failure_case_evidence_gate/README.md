# Failure-case evidence-gate fixtures

The test module derives each named adversarial fixture from the complete valid
record and mutates exactly one gate condition. The fixture names mirror the
task-111 contract: hypothetical phrase, missing target commit/run/trace, LLM
output, wrong semantic target, no external evidence, changed formalization,
deleted first failure, wrong directory placement and a valid reproduced defect.
