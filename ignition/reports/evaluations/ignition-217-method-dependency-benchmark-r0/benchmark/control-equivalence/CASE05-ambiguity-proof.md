# CASE05 ambiguity proof — R1

## Evidence before the cut relation

FACTS and both byte-identical atom blocks give two grouping procedures, a prior cross-access event whose raw record omits the grouping key and procedure version, a present request containing two signatory classes, and no selected procedure or output. LINKLESS retains R02–R05: version chronology, current-shape fields, verification fields, and execution status. None attributes the prior event to either procedure or assigns a scope to the revisions.

## Two materially different licensed actions

1. Propose V0 for the request and verify the output labels.
2. Propose V1 for the request and verify that output groups preserve signatory classes.

Both are consistent with FACTS, retained atoms, and retained relations. The raw cross-access event does not identify its grouping mechanism; V0 preserves request-level grouping, while V1 separates signatory classes. No current export has occurred.

## Missing relation

R01 links the counterexample to V1's limited mixed-signatory scope and preserves V0 for single-class records. Without that source-linked scope relation, the record does not determine which grouping procedure to propose for this request.

TWO_WAY_AMBIGUITY_DEMONSTRATED=YES
