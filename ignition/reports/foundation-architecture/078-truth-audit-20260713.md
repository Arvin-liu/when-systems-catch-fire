# 078 truth audit

## Verdict

`PARTIAL_SEMANTIC_ADJUDICATION`.

The 078 `621/622` number is not an independently verified semantic-review rate. Code inspection of `adjudicate_core.py` shows that all 622 registry classifications pass through one generator: 74 IDs use explicit dictionaries/branches and 548 use title, expression, keyword, or default regex branches. The resulting records then describe themselves as source-text reviews, so the script's own output was incorrectly reused as review evidence.

079 therefore records:

- migration coverage: 622/622;
- preclassification coverage: 622/622;
- hardcoded mapping: 74;
- regex preclassification: 548;
- independently verified source-text semantic review: 5/622;
- internal-component source review: 9/9;
- empirical evidence coverage: 0/622.

The machine-readable sources are `079-method-audit.jsonl` and `079-verified-coverage.json`. Automatic coverage must never be reported as semantic adjudication coverage.
