# IGNITION-150 — Archify bounded visualization admission (Draft)

## Review status

This PR is intentionally Draft-only and must stop at:

`AWAIT_OWNER_ARCHIFY_BOUNDED_ADMISSION_REVIEW`

Do not mark Ready, merge, register a Current capability, select a default
renderer, run authenticated channels, change Agent Reach, or invoke the live
external-execution obligation from this PR.

## Scope

This branch records an Archify-only, read-only, provider-neutral bounded
visualization experiment from the Task149 A8 exact main baseline
`d7372c27abe456b5b8c058675630d8038f91b448`. It adds lineage-bound typed IR,
geometry-only adapter evidence, standalone and Delta visual evidence,
renderer/fallback/environment/license boundaries, registry-gate evidence,
front-door restraint, adversarial authority fixtures and a formal DEFER
decision.

The Current Capability Registry remains unchanged at 19 operations. Archify is
not a Current provider, default renderer, permission source, architecture-truth
authority, external-truth authority or production capability. Provider choice
does not grant permission, and provider output does not change canonical
architecture or semantic relationships.

## Evidence and decision

- Standalone authored visualization: containment PASS across the recorded
  light/dark viewports; perceptual review remains pending.
- Semantic Architecture Delta: 28/28 checks PASS, with no authored component,
  connection or boundary changes.
- Delta visual containment: three `viewer/viewport-overflow` diagnostics remain
  because the upstream compare wrapper has fixed chrome and a `min-height:
  520px` Delta canvas. No upstream source fork, CSS override, overflow hiding
  or concealment workaround was used.
- Registry candidate gate: NOT_REGISTERED; registry write not performed.
- Adversarial authority fixtures: 10/10 escalation mutations rejected in
  read-only evaluation; no provider process, credential/session access or
  system/repository mutation occurred.
- Final bounded admission decision: `DEFER` because the Delta wrapper blocker
  remains and Owner aesthetic acceptance is pending.

The prior Owner context remains `FIT_WITH_LIMITS / CONTINUE_EXPERIMENT` for
Archify and public Agent Reach, while authenticated Agent Reach remains
`DEFER`. These are experiment/review dispositions, not activation or Current
admission.

## Explicit non-intent

`DO_NOT_ACTIVATE_PROVIDER`

`DO_NOT_ADD_CURRENT_PROVIDER_CAPABILITY`

`DO_NOT_ENABLE_AUTHENTICATED_CHANNELS`

`DO_NOT_CHANGE_LIVE_EXTERNAL_INVOCATION`

`MERGE_EXPERIMENTAL_EVIDENCE_AND_PROVIDER_NEUTRAL_CONTRACT_ONLY`

## Requested review

Review the bounded evidence, the six invariant boundaries, the retained Delta
wrapper residual, the visual screenshots and the claim ceiling. If the work is
to proceed later, it requires an explicit Owner visual decision plus a fresh
exact-head re-run after an admissible upstream wrapper resolution; it does not
authorize a follow-on task in this PR.
