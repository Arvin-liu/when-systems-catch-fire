# Task156 prospective paired casebook — 2026-09-05

Status: `FROZEN_BEFORE_SCORING / UNBLINDED_AFTER_TWO_IDENTICAL_PASSES / RESEARCH_ONLY`

This casebook is generated from the frozen answer-key and score outputs. Pre-scoring blind packets contain no roles or truth labels; the answer key is not an input to `score`. Every pair keeps the primary fixture and matched control in the same split.

## F1 — CC-012

CC-012 supplied the consequence/accountability shape; fixtures recombine observer, ownership, stop and retry fields across new receipt variants.

### F1-P01 (calibration)

- Primary truth class: `DEFECT`; matched control: `CONTROL`.
- Diagnostic annotation: `COMPLETE_RECORD_WITHOUT_ACCOUNTABILITY`.
- M0/M3/M3R/M4B primary results: `FLAG` / `FLAG` / `FLAG` / `FLAG`.
- M0/M3/M3R/M4B control results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- Stable matched-pair flip: `PASS`.

### F1-P02 (calibration)

- Primary truth class: `DEFECT`; matched control: `CONTROL`.
- Diagnostic annotation: `COMPLETE_RECORD_WITHOUT_ACCOUNTABILITY`.
- M0/M3/M3R/M4B primary results: `FLAG` / `FLAG` / `FLAG` / `FLAG`.
- M0/M3/M3R/M4B control results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- Stable matched-pair flip: `PASS`.

### F1-P03 (calibration)

- Primary truth class: `DEFECT`; matched control: `CONTROL`.
- Diagnostic annotation: `COMPLETE_RECORD_WITHOUT_ACCOUNTABILITY`.
- M0/M3/M3R/M4B primary results: `FLAG` / `FLAG` / `FLAG` / `FLAG`.
- M0/M3/M3R/M4B control results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- Stable matched-pair flip: `PASS`.

### F1-P04 (holdout)

- Primary truth class: `DEFECT`; matched control: `CONTROL`.
- Diagnostic annotation: `COMPLETE_RECORD_WITHOUT_ACCOUNTABILITY`.
- M0/M3/M3R/M4B primary results: `NO_FLAG` / `FLAG` / `FLAG` / `FLAG`.
- M0/M3/M3R/M4B control results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- Stable matched-pair flip: `PASS`.

### F1-P05 (calibration)

- Primary truth class: `DEFECT`; matched control: `CONTROL`.
- Diagnostic annotation: `COMPLETE_RECORD_WITHOUT_ACCOUNTABILITY`.
- M0/M3/M3R/M4B primary results: `NO_FLAG` / `FLAG` / `FLAG` / `FLAG`.
- M0/M3/M3R/M4B control results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- Stable matched-pair flip: `PASS`.

### F1-P06 (calibration)

- Primary truth class: `DEFECT`; matched control: `CONTROL`.
- Diagnostic annotation: `COMPLETE_RECORD_WITHOUT_ACCOUNTABILITY`.
- M0/M3/M3R/M4B primary results: `NO_FLAG` / `FLAG` / `FLAG` / `FLAG`.
- M0/M3/M3R/M4B control results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- Stable matched-pair flip: `PASS`.

### F1-P07 (calibration)

- Primary truth class: `DEFECT`; matched control: `CONTROL`.
- Diagnostic annotation: `COMPLETE_RECORD_WITHOUT_ACCOUNTABILITY`.
- M0/M3/M3R/M4B primary results: `NO_FLAG` / `FLAG` / `FLAG` / `FLAG`.
- M0/M3/M3R/M4B control results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- Stable matched-pair flip: `PASS`.

### F1-P08 (calibration)

- Primary truth class: `DEFECT`; matched control: `CONTROL`.
- Diagnostic annotation: `COMPLETE_RECORD_WITHOUT_ACCOUNTABILITY`.
- M0/M3/M3R/M4B primary results: `NO_FLAG` / `FLAG` / `FLAG` / `FLAG`.
- M0/M3/M3R/M4B control results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- Stable matched-pair flip: `PASS`.

## F2 — CC-026

CC-026 supplied the Base/Delta scope-coupling shape; fixtures vary provider, lifecycle and object-level admission without copying its historical records.

### F2-P01 (holdout)

- Primary truth class: `DEFECT`; matched control: `CONTROL`.
- Diagnostic annotation: `PROVENANCE_WITHOUT_CEILING`.
- M0/M3/M3R/M4B primary results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `FLAG`.
- M0/M3/M3R/M4B control results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- Stable matched-pair flip: `PASS`.

### F2-P02 (calibration)

- Primary truth class: `DEFECT`; matched control: `CONTROL`.
- Diagnostic annotation: `PROVENANCE_WITHOUT_CEILING`.
- M0/M3/M3R/M4B primary results: `FLAG` / `FLAG` / `FLAG` / `FLAG`.
- M0/M3/M3R/M4B control results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- Stable matched-pair flip: `PASS`.

### F2-P03 (holdout)

- Primary truth class: `DEFECT`; matched control: `CONTROL`.
- Diagnostic annotation: `PROVENANCE_WITHOUT_CEILING`.
- M0/M3/M3R/M4B primary results: `NO_FLAG` / `FLAG` / `FLAG` / `FLAG`.
- M0/M3/M3R/M4B control results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- Stable matched-pair flip: `PASS`.

### F2-P04 (calibration)

- Primary truth class: `DEFECT`; matched control: `CONTROL`.
- Diagnostic annotation: `PROVENANCE_WITHOUT_CEILING`.
- M0/M3/M3R/M4B primary results: `FLAG` / `FLAG` / `FLAG` / `FLAG`.
- M0/M3/M3R/M4B control results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- Stable matched-pair flip: `PASS`.

### F2-P05 (calibration)

- Primary truth class: `DEFECT`; matched control: `CONTROL`.
- Diagnostic annotation: `PROVENANCE_WITHOUT_CEILING`.
- M0/M3/M3R/M4B primary results: `NO_FLAG` / `FLAG` / `FLAG` / `FLAG`.
- M0/M3/M3R/M4B control results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- Stable matched-pair flip: `PASS`.

### F2-P06 (calibration)

- Primary truth class: `DEFECT`; matched control: `CONTROL`.
- Diagnostic annotation: `PROVENANCE_WITHOUT_CEILING`.
- M0/M3/M3R/M4B primary results: `NO_FLAG` / `FLAG` / `FLAG` / `FLAG`.
- M0/M3/M3R/M4B control results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- Stable matched-pair flip: `PASS`.

### F2-P07 (calibration)

- Primary truth class: `DEFECT`; matched control: `CONTROL`.
- Diagnostic annotation: `PROVENANCE_WITHOUT_CEILING`.
- M0/M3/M3R/M4B primary results: `NO_FLAG` / `FLAG` / `FLAG` / `FLAG`.
- M0/M3/M3R/M4B control results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- Stable matched-pair flip: `PASS`.

### F2-P08 (calibration)

- Primary truth class: `DEFECT`; matched control: `CONTROL`.
- Diagnostic annotation: `PROVENANCE_WITHOUT_CEILING`.
- M0/M3/M3R/M4B primary results: `NO_FLAG` / `FLAG` / `FLAG` / `FLAG`.
- M0/M3/M3R/M4B control results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- Stable matched-pair flip: `PASS`.

## F3 — CC-020

CC-020 supplied the identity/projection split-brain challenge; fixtures add novel lifecycle and scope junctions while keeping each local object valid.

### F3-P01 (calibration)

- Primary truth class: `DEFECT`; matched control: `CONTROL`.
- Diagnostic annotation: `PROVENANCE_WITHOUT_CEILING`.
- M0/M3/M3R/M4B primary results: `NO_FLAG` / `NO_FLAG` / `FLAG` / `FLAG`.
- M0/M3/M3R/M4B control results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- Stable matched-pair flip: `PASS`.

### F3-P02 (holdout)

- Primary truth class: `DEFECT`; matched control: `CONTROL`.
- Diagnostic annotation: `PROVENANCE_WITHOUT_CEILING`.
- M0/M3/M3R/M4B primary results: `NO_FLAG` / `NO_FLAG` / `FLAG` / `FLAG`.
- M0/M3/M3R/M4B control results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- Stable matched-pair flip: `PASS`.

### F3-P03 (holdout)

- Primary truth class: `DEFECT`; matched control: `CONTROL`.
- Diagnostic annotation: `PROVENANCE_WITHOUT_CEILING`.
- M0/M3/M3R/M4B primary results: `NO_FLAG` / `NO_FLAG` / `FLAG` / `FLAG`.
- M0/M3/M3R/M4B control results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- Stable matched-pair flip: `PASS`.

### F3-P04 (calibration)

- Primary truth class: `DEFECT`; matched control: `CONTROL`.
- Diagnostic annotation: `PROVENANCE_WITHOUT_CEILING`.
- M0/M3/M3R/M4B primary results: `NO_FLAG` / `NO_FLAG` / `FLAG` / `FLAG`.
- M0/M3/M3R/M4B control results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- Stable matched-pair flip: `PASS`.

### F3-P05 (calibration)

- Primary truth class: `DEFECT`; matched control: `CONTROL`.
- Diagnostic annotation: `PROVENANCE_WITHOUT_CEILING`.
- M0/M3/M3R/M4B primary results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `FLAG`.
- M0/M3/M3R/M4B control results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- Stable matched-pair flip: `PASS`.

### F3-P06 (calibration)

- Primary truth class: `DEFECT`; matched control: `CONTROL`.
- Diagnostic annotation: `PROVENANCE_WITHOUT_CEILING`.
- M0/M3/M3R/M4B primary results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `FLAG`.
- M0/M3/M3R/M4B control results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- Stable matched-pair flip: `PASS`.

### F3-P07 (holdout)

- Primary truth class: `DEFECT`; matched control: `CONTROL`.
- Diagnostic annotation: `PROVENANCE_WITHOUT_CEILING`.
- M0/M3/M3R/M4B primary results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `FLAG`.
- M0/M3/M3R/M4B control results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- Stable matched-pair flip: `PASS`.

### F3-P08 (calibration)

- Primary truth class: `DEFECT`; matched control: `CONTROL`.
- Diagnostic annotation: `PROVENANCE_WITHOUT_CEILING`.
- M0/M3/M3R/M4B primary results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `FLAG`.
- M0/M3/M3R/M4B control results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- Stable matched-pair flip: `PASS`.

## F4 — Task155 claim-ceiling map

The claim-to-action shape is derived from current claim ceiling and evidence-lineage contracts; it is not a replay of a historical CC case.

### F4-P01 (calibration)

- Primary truth class: `DEFECT`; matched control: `CONTROL`.
- Diagnostic annotation: `PROVENANCE_WITHOUT_CEILING`.
- M0/M3/M3R/M4B primary results: `FLAG` / `FLAG` / `FLAG` / `FLAG`.
- M0/M3/M3R/M4B control results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- Stable matched-pair flip: `PASS`.

### F4-P02 (calibration)

- Primary truth class: `DEFECT`; matched control: `CONTROL`.
- Diagnostic annotation: `PROVENANCE_WITHOUT_CEILING`.
- M0/M3/M3R/M4B primary results: `FLAG` / `FLAG` / `FLAG` / `FLAG`.
- M0/M3/M3R/M4B control results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- Stable matched-pair flip: `PASS`.

### F4-P03 (calibration)

- Primary truth class: `DEFECT`; matched control: `CONTROL`.
- Diagnostic annotation: `PROVENANCE_WITHOUT_CEILING`.
- M0/M3/M3R/M4B primary results: `FLAG` / `FLAG` / `FLAG` / `FLAG`.
- M0/M3/M3R/M4B control results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- Stable matched-pair flip: `PASS`.

### F4-P04 (calibration)

- Primary truth class: `DEFECT`; matched control: `CONTROL`.
- Diagnostic annotation: `PROVENANCE_WITHOUT_CEILING`.
- M0/M3/M3R/M4B primary results: `FLAG` / `FLAG` / `FLAG` / `FLAG`.
- M0/M3/M3R/M4B control results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- Stable matched-pair flip: `PASS`.

### F4-P05 (calibration)

- Primary truth class: `DEFECT`; matched control: `CONTROL`.
- Diagnostic annotation: `PROVENANCE_WITHOUT_CEILING`.
- M0/M3/M3R/M4B primary results: `NO_FLAG` / `FLAG` / `FLAG` / `FLAG`.
- M0/M3/M3R/M4B control results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- Stable matched-pair flip: `PASS`.

### F4-P06 (holdout)

- Primary truth class: `DEFECT`; matched control: `CONTROL`.
- Diagnostic annotation: `PROVENANCE_WITHOUT_CEILING`.
- M0/M3/M3R/M4B primary results: `NO_FLAG` / `FLAG` / `FLAG` / `FLAG`.
- M0/M3/M3R/M4B control results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- Stable matched-pair flip: `PASS`.

### F4-P07 (holdout)

- Primary truth class: `DEFECT`; matched control: `CONTROL`.
- Diagnostic annotation: `PROVENANCE_WITHOUT_CEILING`.
- M0/M3/M3R/M4B primary results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `FLAG`.
- M0/M3/M3R/M4B control results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- Stable matched-pair flip: `PASS`.

### F4-P08 (calibration)

- Primary truth class: `DEFECT`; matched control: `CONTROL`.
- Diagnostic annotation: `PROVENANCE_WITHOUT_CEILING`.
- M0/M3/M3R/M4B primary results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `FLAG`.
- M0/M3/M3R/M4B control results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- Stable matched-pair flip: `PASS`.

## F5 — Task155 consequence/rollback map

The rollback shape comes from existing reversible local-file controls and durability recovery contracts, with synthetic irreversible and stop-path combinations.

### F5-P01 (holdout)

- Primary truth class: `DEFECT`; matched control: `CONTROL`.
- Diagnostic annotation: `COMPLETE_RECORD_WITHOUT_ACCOUNTABILITY`.
- M0/M3/M3R/M4B primary results: `NO_FLAG` / `FLAG` / `FLAG` / `FLAG`.
- M0/M3/M3R/M4B control results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- Stable matched-pair flip: `PASS`.

### F5-P02 (calibration)

- Primary truth class: `DEFECT`; matched control: `CONTROL`.
- Diagnostic annotation: `COMPLETE_RECORD_WITHOUT_ACCOUNTABILITY`.
- M0/M3/M3R/M4B primary results: `FLAG` / `FLAG` / `FLAG` / `FLAG`.
- M0/M3/M3R/M4B control results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- Stable matched-pair flip: `PASS`.

### F5-P03 (calibration)

- Primary truth class: `DEFECT`; matched control: `CONTROL`.
- Diagnostic annotation: `COMPLETE_RECORD_WITHOUT_ACCOUNTABILITY`.
- M0/M3/M3R/M4B primary results: `FLAG` / `FLAG` / `FLAG` / `FLAG`.
- M0/M3/M3R/M4B control results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- Stable matched-pair flip: `PASS`.

### F5-P04 (calibration)

- Primary truth class: `DEFECT`; matched control: `CONTROL`.
- Diagnostic annotation: `COMPLETE_RECORD_WITHOUT_ACCOUNTABILITY`.
- M0/M3/M3R/M4B primary results: `FLAG` / `FLAG` / `FLAG` / `FLAG`.
- M0/M3/M3R/M4B control results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- Stable matched-pair flip: `PASS`.

### F5-P05 (calibration)

- Primary truth class: `DEFECT`; matched control: `CONTROL`.
- Diagnostic annotation: `COMPLETE_RECORD_WITHOUT_ACCOUNTABILITY`.
- M0/M3/M3R/M4B primary results: `NO_FLAG` / `FLAG` / `FLAG` / `FLAG`.
- M0/M3/M3R/M4B control results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- Stable matched-pair flip: `PASS`.

### F5-P06 (calibration)

- Primary truth class: `DEFECT`; matched control: `CONTROL`.
- Diagnostic annotation: `COMPLETE_RECORD_WITHOUT_ACCOUNTABILITY`.
- M0/M3/M3R/M4B primary results: `NO_FLAG` / `FLAG` / `FLAG` / `FLAG`.
- M0/M3/M3R/M4B control results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- Stable matched-pair flip: `PASS`.

### F5-P07 (calibration)

- Primary truth class: `DEFECT`; matched control: `CONTROL`.
- Diagnostic annotation: `COMPLETE_RECORD_WITHOUT_ACCOUNTABILITY`.
- M0/M3/M3R/M4B primary results: `NO_FLAG` / `FLAG` / `FLAG` / `FLAG`.
- M0/M3/M3R/M4B control results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- Stable matched-pair flip: `PASS`.

### F5-P08 (holdout)

- Primary truth class: `DEFECT`; matched control: `CONTROL`.
- Diagnostic annotation: `COMPLETE_RECORD_WITHOUT_ACCOUNTABILITY`.
- M0/M3/M3R/M4B primary results: `NO_FLAG` / `FLAG` / `FLAG` / `FLAG`.
- M0/M3/M3R/M4B control results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- Stable matched-pair flip: `PASS`.

## F6 — Task155 signer/contestability diagnostic

The authorization shape stress-tests the weak signer/contestability label; four cases intentionally omit a consequence failure so the label cannot be manufactured.

### F6-P01 (holdout)

- Primary truth class: `DEFECT`; matched control: `CONTROL`.
- Diagnostic annotation: `SIGNATURE_WITHOUT_CONTESTABILITY`.
- M0/M3/M3R/M4B primary results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `FLAG`.
- M0/M3/M3R/M4B control results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- Stable matched-pair flip: `PASS`.

### F6-P02 (calibration)

- Primary truth class: `DEFECT`; matched control: `CONTROL`.
- Diagnostic annotation: `SIGNATURE_WITHOUT_CONTESTABILITY`.
- M0/M3/M3R/M4B primary results: `FLAG` / `FLAG` / `FLAG` / `FLAG`.
- M0/M3/M3R/M4B control results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- Stable matched-pair flip: `PASS`.

### F6-P03 (calibration)

- Primary truth class: `DEFECT`; matched control: `CONTROL`.
- Diagnostic annotation: `SIGNATURE_WITHOUT_CONTESTABILITY`.
- M0/M3/M3R/M4B primary results: `NO_FLAG` / `FLAG` / `FLAG` / `FLAG`.
- M0/M3/M3R/M4B control results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- Stable matched-pair flip: `PASS`.

### F6-P04 (holdout)

- Primary truth class: `DEFECT`; matched control: `CONTROL`.
- Diagnostic annotation: `SIGNATURE_WITHOUT_CONTESTABILITY`.
- M0/M3/M3R/M4B primary results: `NO_FLAG` / `FLAG` / `FLAG` / `FLAG`.
- M0/M3/M3R/M4B control results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- Stable matched-pair flip: `PASS`.

### F6-P05 (calibration)

- Primary truth class: `DEFECT`; matched control: `CONTROL`.
- Diagnostic annotation: `SIGNATURE_WITHOUT_CONTESTABILITY`.
- M0/M3/M3R/M4B primary results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `FLAG`.
- M0/M3/M3R/M4B control results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- Stable matched-pair flip: `PASS`.

### F6-P06 (calibration)

- Primary truth class: `DEFECT`; matched control: `CONTROL`.
- Diagnostic annotation: `SIGNATURE_WITHOUT_CONTESTABILITY`.
- M0/M3/M3R/M4B primary results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `FLAG`.
- M0/M3/M3R/M4B control results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- Stable matched-pair flip: `PASS`.

### F6-P07 (calibration)

- Primary truth class: `AMBIGUOUS_STRESS`; matched control: `CONTROL`.
- Diagnostic annotation: `SIGNATURE_WITHOUT_CONTESTABILITY`.
- M0/M3/M3R/M4B primary results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- M0/M3/M3R/M4B control results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- Stable matched-pair flip: `NO_FLIP_OR_STRESS`.

### F6-P08 (calibration)

- Primary truth class: `AMBIGUOUS_STRESS`; matched control: `CONTROL`.
- Diagnostic annotation: `SIGNATURE_WITHOUT_CONTESTABILITY`.
- M0/M3/M3R/M4B primary results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- M0/M3/M3R/M4B control results: `NO_FLAG` / `NO_FLAG` / `NO_FLAG` / `NO_FLAG`.
- Stable matched-pair flip: `NO_FLIP_OR_STRESS`.
