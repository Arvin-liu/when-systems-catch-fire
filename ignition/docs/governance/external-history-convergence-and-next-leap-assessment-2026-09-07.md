# IGNITION-20260907-162: convergence and next-leap assessment

## Combined decision

The final machine verdict is:

- input regime: `INFORMATION_VOLUME_EFFECT_ONLY`;
- historical track: `HISTORICAL_UNDERDETERMINED`;
- basis/semantic verdict: `NO_BASIS_ESCAPE_DETECTED`;
- epistemic status: `DETECTOR_NOT_VALIDATED / UNDERDETERMINED`;
- lifecycle: research-only, Draft-only, not Ready, not Current, not merged, and not Owner-accepted.

There is no independent convergence between the external and historical tracks. They use separate source channels, but the external rows are generic lexical/argument summaries and the historical labels depend on explicit repository markers. `cross-source-convergence.jsonl` records `NO_INDEPENDENT_CONVERGENCE_ESTABLISHED`; the procedural two-pass arrangement is explicitly not cognitive independence.

## Answers to the two strongest hypotheses

The volume ladder rises from a mean of 9.545455 factors at 10% to 9.954545 at 50%, then remains at 9.954545 through 100%. This is compatible with a bounded information-volume effect and a measurement ceiling. The O/S/F order ablation has 16 paired discovery comparisons, with original order beating both controls in only 5 (`31.25%`), below the 60% support threshold. Thus the evidence does not support a distinct logical-coherence effect.

No external candidate passes the Task159 V2 gate. All ten candidates have `L2`, `L3`, `L4`, and `L6` not established, and no candidate has independent canonical validation. The external/history crosswalk contains no shared lost question and no established semantic-leap status. Candidate deletion files are method-sensitivity observations only.

## Inherited Task161 residual

Formal PR #211 for Task161 was verified open and Draft at head `5ccb15d45cec259d5397f1843278fd98011105aa`, based on `work/IGNITION-20260907-160`. Its inherited CI record contains successful path-accounting and architecture checks but a preserved Foundation residual: `CENSUS_OUT_OF_DATE / NONFUNCTION_CLAIM_OUTPUT_DRIFT`. Task162 records this separately and does not rerun, repair, or reinterpret it as a Task162 result.

The required `instructions/CURRENT.md` and `relay/current` pointers were absent in the exact Formal base. That condition is recorded as `STALE_CONTROL_POINTER / PREFLIGHT_RESIDUAL`; no pointer was created, repaired, or redirected.

## Next semantic leap

No validated next semantic leap can be named from this run. The strongest negative findings are (1) the coherence advantage is absent in the frozen paired comparison, (2) the volume ladder plateaus, (3) all external candidates fail V2 independence/semantic gates, and (4) the historical adapter has a large false-positive burden with only 72 clean/strong negatives. A future study would need independently frozen semantic adjudication, fresh negative controls, a complete canonical validator invocation, and exact-head remote CI before any basis or lifecycle claim could be reconsidered. No Task163 was created.

The controlling summary is `verdict.json`; the final V2 gate is `v2-final-gate.json`; exact command, base, pointer, and lifecycle provenance is in `freeze-ledger.json`.
