# Generator Plasticity Audit — IGNITION-20260906-158

Status: research-only audit. The phrase `GENERATOR_LOCK_IN_SUPPORTED_AS_RESEARCH_FINDING` is retained inside the bounded machine audit, but it is not a validated causal verdict because the historical detector failed its pre-registered negative holdout.

## Current observed pipeline

The inspected default path is:

`source → classify → evidence/claim → registry → validation → projection → publication/governance`

The main evidence paths were:

- `ignition/tools/foundation/build_function_asset_census.py`
- `ignition/tools/foundation/adjudicate_nonfunction_claims.py`
- `ignition/tools/governance/build_knowledge_experience.py`
- `ignition/tools/publication/build_fire_seed_census.py`

## Basis-mutation operation audit

| Operation | Observed default route | Research audit status |
| --- | --- | --- |
| create a new object class | candidate/registry and validator | path exists; not a basis replacement |
| distinguish a new object from an object-language change | not in default generator path | research-only |
| invalidate a canonical representation | archive/migration plus manual review | partial |
| merge axes | new combination or projection | not observed |
| preserve identity while changing ontology | migration/source lineage | partial |
| replace Cartesian representation with relation/transition | research overlay only | not observed in default path |
| rerun old corpus under a competing basis | ordinary regeneration under current basis | not observed |
| retire a basis axis | new local rule or exclusion | not observed |
| split a basis axis | new field or object | not observed |

This pattern supports a bounded research observation: ordinary maintenance destinations are new local rules, validators, schema fields, candidate objects, or projections. It does not prove that a new production operator is required or authorized.

## Backflow boundary

The task's research data directory is retained in repository path accounting but excluded from Foundation discovery/admission. The following task-scoped boundaries were added without weakening validators:

- `data/research/basis-escape-meta-plasticity-2026-09-06/` is excluded from function-asset discovery and non-function claim admission while remaining source-discovery-accounted;
- the five governance documents, the task report, and the formal result are excluded from those claim/asset inputs for the same provenance reason;
- the exclusion is scoped to this research surface and does not change the general validator contract.

The current task's research tool/data are also excluded from basis-free induction itself, so the experiment cannot count its own implementation as an independent discovery.

## Audit conclusion

The audit finds an absence/partiality of basis-mutation routes in the default workflow and a repeated generator/projection residual. Since N03 is a negative holdout false positive, the final task verdict remains `DETECTOR_NOT_VALIDATED / UNDERDETERMINED`. No schema, registry, mandatory validator, runtime, permission, authority, or production gate was changed.

Machine detail is in `ignition/data/research/basis-escape-meta-plasticity-2026-09-06/generator-plasticity-audit.json`.
