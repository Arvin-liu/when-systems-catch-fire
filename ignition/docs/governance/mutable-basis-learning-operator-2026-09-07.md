# Task163 mutable basis-learning operator

Status: research-only, Draft-only, and stopped at the historical qualification gate. This package does not change canonical protocol meaning, the 64 matrix, runtime behavior, validators, provider semantics, authority, Current state, production readiness, or Owner acceptance.

## Provenance and exact baseline

The controlling command was read from `Arvin-liu/1111` at:

- commit: `73cde2ab84d829bb0990f63d957e2152b1a330e4`
- Git blob: `521d468829425a210cc763df72456f2c28753d02`
- complete-content SHA-256: `f4834b50cf26688e03e5655134d0d12019b8548093752f33b2454869b26a48a1`

Formal execution began at `work/IGNITION-20260907-162@5c66ac502ca331d72c1362000ff5231503dde7ea`. PR #212 was rechecked as OPEN + DRAFT with head `5c66ac502ca331d72c1362000ff5231503dde7ea` and base `work/IGNITION-20260907-161`. The exact-base worktree was clean. `instructions/CURRENT.md` and `relay/current` were absent and were preserved as `STALE_CONTROL_POINTER / PREFLIGHT_RESIDUAL`.

## Frozen operator boundary

R1 stores primitives, relation types and arity, composition grammar, allowed operators, question forms, falsifiers, identity/scope assumptions, residuals, and complexity. It does not assume a named formal basis. It extracts bounded claims, reasons, evidence, constraints, exceptions, revisions, contradictions, questions, and dependency markers from pre-event repository material.

The historical operator input contains only cutoff-before material from 18 real repository events. It contains no event labels, post-event refs, post-event files, answer descriptions, Task159/Task162 induction results, or external V2 scores. The answer key is a separate artifact and is opened only after both blind output files are written. This is procedural separation only; the same process produced the operator and the answer key.

All 12 mutation operations were applied to a neutral trial state and recorded as `TRIAL_APPLIED` with `semantic_claim: NONE`. No trial is a candidate, and no trial was used to tune the qualification gate.

The frozen trigger requires independent-work and at-least-two-domain recurrence, or one of the other preregistered residual/complexity triggers. The historical packets do not expose independent domain identities, so recurrent residuals were recorded but blocked from becoming mutations. No mutation survived a full replay.

## Stage A result

| Gate | Result |
| --- | --- |
| True leap capability-equivalent signals | `0/4`, required `>=3/4` |
| P02/P03/P04 capability-equivalent signals | `0/3`, required `>=2/3` |
| Strong-negative false positives | `0`, required `0` |
| N02/N03 conservative behavior | stable in this run |
| True holdout hit | `0`, required `>=1` |
| Two blind replay bytes | identical |

The operator produced generic structural signals in some snapshots, but no representation mutation with full-replay survival and no capability-equivalent signal. The failed gates are therefore the positive recall gate, the P02/P03/P04 gate, and the true-holdout-hit gate. This establishes `BASIS_LEARNING_OPERATOR_NOT_VALIDATED`, not a negative claim about the historical events themselves.

## Stop rule

Because Stage A failed, Stage B was not run. No Task162 longform induction output was reused as a generator input; no external text was acquired; no fresh holdout was selected; no O/S/B/R ablation, F0/M1 comparison, multi-pass discovery, candidate freeze, existing-basis crosswalk, or Task159 V2 scoring was performed. All Stage B filenames are explicit `NOT_RUN_STAGE_A_STOP` sentinels rather than empty or implied evidence.

The machine ledger is `ignition/data/research/mutable-basis-learning-operator-2026-09-07/freeze-ledger.json`; the primary result is `verdict.json`.
