#!/bin/sh
set -eu
repo_root=$(git rev-parse --show-toplevel)
guard="$repo_root/ignition/evaluation/tools/task_branch_guard_r0_3.py"
if [ -f "$guard" ]; then
  exec python3 "$guard" --operation COMMIT
fi
printf '%s\n' FAIL_AUTHORIZATION_RECEIPT >&2
exit 1
