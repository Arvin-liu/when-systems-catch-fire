# Task182-R2 Bootstrap Boundary

The future Task182-R2 Successor must start only after a separately authorized
R2 task command and clone-local authorization receipt bind the exact final
R1 handoff commit, this repository, the R2 branch, origin, and the public
GitHub noreply identity. This packet and its branch template do not themselves
authorize a commit or push. Do not change global Git configuration.

Before the first write, record the exact repository, origin and push URL,
branch, head, base ancestry, local identity, hook path, and explained worktree
state. Run the installed guard through Git hooks or its finite status
interface. Do not read the hook, guard, schema, template, or authorization
receipt source/content.

Only the cognitive files in the packet manifest may be opened for task
reasoning. Operational controls may execute and return only their listed
status codes. The manifest and read-ledger schema are boundary objects whose
reads must be recorded. Do not search for or open any other repository path.
