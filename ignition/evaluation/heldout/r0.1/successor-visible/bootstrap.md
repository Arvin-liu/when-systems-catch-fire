# Successor Bootstrap Boundary

Before searching or writing, the future executor must establish that the
checkout origin is `Arvin-liu/when-systems-catch-fire`, verify the task's exact
base and head refs, confirm a clean explained worktree, and set a repository-
local public GitHub noreply author identity. A wrong repository or invalid
identity must stop the task before its first commit. Do not change global Git
identity.

Task182 must use its own authorized branch and task-specific command. This
packet does not authorize reusing Task181's branch, writing to a historical
branch, or reading files outside `packet-manifest.json` and the separately
listed read-ledger schema. Record observable checks and results; do not save
hidden reasoning traces.
