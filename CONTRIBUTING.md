# 贡献指南

## 目标
欢迎任何有助于让项目更清晰、更可验证、更易读的贡献，尤其是新的故事案例、函数条目、测试、反例和数据接入说明。

## 如何提交案例 / 函数
- 新故事放入 `新故事/`，新案例放入 `统一案例总表/`，新函数放入 `统一函数总表/`。
- 文件名尽量保持简洁、可检索，并尽量沿用现有编号风格。
- 每个条目都建议包含：标题、摘要、关键对象、适用范围、与已有条目的关系。
- 如果是函数条目，尽量同步说明它和 `Ψ₀` 六个构件中的哪一部分有关。

## 如何提交反例
- 反例和失败案例请优先参考 [docs/falsifiability/README.md](./docs/falsifiability/README.md) 中的模板。
- 建议写清楚：假设、场景、预测、评估方法、结果、备注。
- 如果证据不足，请明确标记为 `pending`，不要强行写成通过。

## 如何提交改进建议
- 先用一个简短的 Pull Request 说明你想改什么、为什么改、影响哪些文件。
- 如果是结构性改动，建议附上示例、截图或最小复现路径。
- 尽量保持改动范围清晰，方便后续审阅和回滚。

## 行为守则
- 欢迎讨论不同观点，但请尽量围绕证据、结构和可验证性展开。
- 避免无端争论、人格化攻击或把技术问题变成立场对抗。
- 目标是一起把项目做得更可靠，而不是证明谁更对。
