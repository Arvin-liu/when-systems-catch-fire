# 碰撞任务模板

## 任务名称

（填写，如：P1 接入烟雾测试）

## 碰撞对象

（填写碰撞对象或主题）

## 输入材料

（贴正文，或说明本地文件路径。不得只给 URL）

## 本地输入文件

（如 inputs/collisions/YYYYMMDD-task-name/source.md）

## 使用函数范围

默认：全量函数表。

## 使用案例范围

默认：全量案例表。

## 是否使用 P1 机器数据

默认：是。

## 输出要求

（列出必须输出的五类 + 报告）

## 不得做的事

（列出禁止项，如：不得修改正式两张表、不得生成大规模报告）

## 保存位置

（如 outputs/collisions/YYYYMMDD-task-name/）
