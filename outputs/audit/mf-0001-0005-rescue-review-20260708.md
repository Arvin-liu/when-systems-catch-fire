# MF-0001~0005 Codespace 救援复核

复核时间：2026-07-08

## 输入

- 救援函数表：`/Users/zhiyuan/Agent 工作区/Codespace-Rescue/Unified-Case-Table/统一函数总表_Codespace救援.md`
- 正式函数表目录：`/Users/zhiyuan/Agent 工作区/Codex/2026-06-25/github-cp-agent-500-600-1000/when-systems-catch-fire/统一函数总表/`

## 复核对象

| ID | 名称 | 救援表是否存在 | 正式表是否存在独立条目 | 是否被 D141 / MF-0000 语义覆盖 | 建议 |
|---|---|---|---|---|---|
| MF-0001 | 正向自举通道 | 是（Section 0 内部子表） | 否 | 否（D141 仅对自身做正向检查，未定义此通道构件） | 补入 |
| MF-0002 | 反向自举通道 | 是（Section 0 内部子表） | 否 | 否（D141 仅对自身做反向检查，未定义此通道构件） | 补入 |
| MF-0003 | 正反互斥判定器 | 是（Section 0 内部子表） | 否 | 否（正式表无 J⁺=J⁻=1 互斥判定的独立构件） | 补入 |
| MF-0004 | 自举嵌套判定器 | 是（Section 0 内部子表） | 否 | 否（正式表无迭代 M_boot 的嵌套判定构件） | 补入 |
| MF-0005 | 自举收敛判定器 | 是（Section 0 内部子表） | 否 | 否（D141 有收敛判据但无独立收敛判定器条目） | 补入 |

## 抽取内容（来自救援函数表 Section 0 内部元算子表）

| ID | 中文名 / 英文名 | 作用 |
|---|---|---|
| MF-0001 | 正向自举通道 / Forward Bootstrap Channel | 计算 J⁺ / Computes J+ |
| MF-0002 | 反向自举通道 / Reverse Bootstrap Channel | 计算 J⁻ / Computes J- |
| MF-0003 | 正反互斥判定器 / Forward-Reverse Exclusivity Judge | 排除 J⁺=J⁻=1 / Rejects J+=J-=1 |
| MF-0004 | 自举嵌套判定器 / Nested Bootstrap Judge | 迭代 M_boot / Iterates M_boot |
| MF-0005 | 自举收敛判定器 / Bootstrap Convergence Judge | 判定不动点 / Tests fixed point |

## 正式表搜索证据

在正式函数表目录中搜索以下关键词，命中情况：

- `正向自举通道`：0 文件
- `反向自举通道`：0 文件
- `正反互斥判定器`：0 文件
- `自举嵌套判定器`：0 文件
- `自举收敛判定器`：0 文件
- `计算 J⁺` / `计算 J⁻`：0 文件
- `排除 J⁺=J⁻`：0 文件
- `不动点`：1 文件（顺带提及，非本层结构）

正式表自举相关文件：`0024-T14-自举元函数层级.md`、`0190-D141-自举元函数.md`、`0001-Ψ₀元函数完整数学定义.md` 等。
其中 `D141-自举元函数.md` 内含 `J_n^+(F_D141)=1`、`J_n^-(F_D141)=0`、`Converged(F_D141)⇔...` 等表述，但这是**对 D141 自身函数做正反向收敛检查**，并非把 MF-0001~0005 定义为 MF-0000 的内部子通道构件。

## 结论

选 **第 2 类：5 个 MF 子项有独立结构价值，建议补入正式函数表 Section 0**。

理由：
1. 这 5 个不是普通新增函数，而是 Section 0 自举元函数（MF-0000）的**内部结构件**——正向/反向通道、互斥判定、嵌套判定、收敛判定。
2. 正式表只以 `D141-自举元函数` 等**上层整体形态**覆盖自举元函数，未保留这层内部子通道拆分，也无任何等价独立条目。
3. 编号无冲突（MF-0001~0005 在正式表完全空缺），标题无冲突，补入不会与现有条目撞车。
4. 与之前差异审计结论一致：函数表救援独有增量仅这 5 个，案例表无增量。

## 补入建议方式（仅建议，未执行）

在正式函数表 `统一函数总表/` 下为 MF-0001~0005 各建独立条目文件（如 `0000-MF-0001-正向自举通道.md` 等），并在 MF-0000 / 自举元函数相关文件中增加"内部子通道"引用小节，与 D141 做语义去重（D141 描述整体自举元函数，MF-0001~0005 描述其内部 5 个算子，层次不同、不重复）。

## 注意

本任务只生成复核报告，不修改正式函数表，不删除 Codespace。是否真正补入，待下一步人工确认。
