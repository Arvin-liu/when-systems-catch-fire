# 跨域 smoke test 审计

## 测试范围

验证「不同领域材料 → 统一两张表碰撞」流程可运行性，并确认框架跨域同构识别能力。

## 环境确认

- git status：干净
- git pull --ff-only：Already up to date
- P1 校验器：`ALL_P1_DATA_VALID`

## 输入（本地文本，避免网页链接依赖）

- `inputs/collisions/20260708-cross-domain-smoke-test/social.md`（社会学·邻里积分制）
- `inputs/collisions/20260708-cross-domain-smoke-test/science.md`（自然科学·高通量 p 值考核）
- `inputs/collisions/20260708-cross-domain-smoke-test/history.md`（历史学·修志数字化积分）

## 流程执行核对

| 步骤 | 结果 |
|---|---|
| 建输入目录 + 写本地材料 | ✓ |
| 两张表查重（函数表 A/B/C/D/E 五组关键词） | ✓ |
| 两张表查重（案例表四组领域关键词） | ✓ |
| 逐领域 Ψ₀ 六维初判 | ✓ |
| 输出三领域碰撞报告 | ✓ |
| 输出跨域同构汇总 | ✓ |
| 只输出候选，未回填 | ✓（本测试边界） |

## 跨域同构识别结果

- 三个领域主机制均命中 **D597 量化指标替代真实价值**。
- 同构结构一致：「指标化评价作为唯一显式入口 → 不可量化真实价值被挤出 + 表演/刷分态出现」。
- 框架跨域同构识别能力：**通过**。

## 新候选函数（跨域复现，待复核）

- NF-X1 指标排名隐性分层（三领域复现，现有表零精确命中）
- NF-X2 刷分博弈（三领域复现，现有表零精确命中）
- NF-X3 指标驱动噪声累积（仅自然科学强出现）

## 流程可运行性结论

smoke test **通过**：不同领域材料可正常接入，查重/Ψ₀/候选输出链路完整，跨域同构可被框架识别。

## 未处理项

- NF-X1 / NF-X2 / NF-X3：仅标记候选，未回填（需后续显式指令 + 查重 + Ψ₀ 判定 + 正式编号）。
- 三领域候选案例：仅标记，未回填。

## 未修改事项

- 未新增函数
- 未新增案例
- 未修改函数表
- 未修改案例表
- 未修改 data
- 未修改 schema
- 未启动 UNESCO
- 未启动得到笔记大规模任务
