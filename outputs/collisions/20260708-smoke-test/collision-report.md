# 碰撞输出报告 · P1 接入烟雾测试

> 本文件为 **smoke test**，仅验证 P1 机器数据接入碰撞流程是否可运行，不追求高质量洞见，不回填正式两张表。

## 任务信息

- 任务名：P1 接入烟雾测试
- 时间：2026-07-08
- 执行：Agent（管线验证）

## 输入来源

- 本地文件：`inputs/collisions/20260708-smoke-test/task.md`
- 无网页链接，符合「默认得到大脑读不了网页链接」的输入原则。

## 使用资产

- 函数表目录：存在（`统一函数总表/`），本轮未读全量正文。
- 案例表目录：存在（`统一案例总表/`），本轮未读全量正文。
- P1 数据：七类全部加载，校验通过 `ALL_P1_DATA_VALID`。
- 校验器：`python3 tools/validate_data.py` 通过。

## 概念拆解

输入描述了一个私有工程流程案例：主线仓库 / 临时救援仓库 / 本地备份三层结构 → 差异审计 → 只补回增量（五个内部结构件）→ 删除临时环境、保留救援分支。核心可判定结构：**分层存储、差异审计优先、只回填增量、凭证保留**。

## 相关经典问题

- classic_problems_benchmark 轻量命中：无直接标题命中（输入为私有流程案例，非学科经典问题）。
- 结论：未命中时流程正常输出，不报错。

## 相关函数

- 本轮未读全量函数表正文（轻量流程），不列具体命中函数。
- 可挂接参考（来自 function_dependency）：结构件层级参考 `FUNC-L0-*`（L0 入口组件）。

## 相关案例

- 本轮未读全量案例表正文，不列具体命中案例。

## 证据制度约束

- evidence_regimes 覆盖领域：数学/物理学/历史学/社会科学/经济学/工程学/医学/法学等。
- 本案例为工程/系统工程实践，属 `工程学` 证据制度范围；因属私有流程复盘，无外部证据需求，claim level 可定为叙述级（非证明级）。

## 风险与 failure typology

轻量索引命中相关失败类型（用于流程风险提示，非判定输入错误）：

- FAIL-001 材料错误：提醒后续真实碰撞须确认输入材料来源清晰。
- FAIL-003 尺度错配：提醒不要把这个私有流程案例误推为通用学科结论。
- FAIL-010 观察者位置错误：提醒复盘时区分「操作者视角」与「框架客观判定」。

## 新增函数

- 见 `new-functions.md`：**本轮不建议新增函数**。

## 新增案例

- 见 `new-cases.md`：**本轮不建议新增案例**。

## 新增注释

- 见 `notes.md`。

## 扩展注释

- 见 `expanded-notes.md`。

## 不采纳项

- 见 `rejected.md`。

## 回填建议

- 见 `backfill-plan.md`：**本轮无回填**。

## 待人工复核

- 无（smoke test 仅验证流程）。

## 下一步

- 若本 smoke test 通过（流程可运行），下一步选一个**真实小材料**做首个正式碰撞，仍不回填或仅小批量回填。
