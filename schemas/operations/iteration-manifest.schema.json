{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.invalid/ignition/operations/iteration-manifest.schema.json",
  "title": "Ignition Iteration Manifest",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "method_version",
    "task_id",
    "change_classification",
    "verified_start",
    "branch_pr",
    "gap",
    "claim_ceiling",
    "status",
    "impact_matrix",
    "changed_surfaces",
    "validation",
    "rollback_strategy",
    "remaining_limitations",
    "receipt_location"
  ],
  "properties": {
    "method_version": { "type": "string", "minLength": 1 },
    "task_id": { "type": "string", "pattern": "^[A-Za-z0-9._-]+$" },
    "change_classification": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "enum": [
          "REPAIR",
          "CAPABILITY_ADDITION",
          "INTERFACE_CHANGE",
          "EVIDENCE_UPDATE",
          "GOVERNANCE_CHANGE",
          "RELEASE_OR_CURRENT_STATE_SYNC",
          "HISTORICAL_ONLY",
          "OPERATIONS_METHOD"
        ]
      },
      "uniqueItems": true
    },
    "verified_start": {
      "type": "object",
      "additionalProperties": false,
      "required": ["main_head", "source", "verified_at"],
      "properties": {
        "main_head": { "$ref": "#/$defs/sha" },
        "source": { "type": "string", "minLength": 1 },
        "verified_at": { "type": "string", "minLength": 1 }
      }
    },
    "branch_pr": {
      "type": "object",
      "additionalProperties": false,
      "required": ["branch", "pr_number", "base", "draft", "merged"],
      "properties": {
        "branch": { "type": "string", "minLength": 1 },
        "pr_number": { "type": ["integer", "null"], "minimum": 1 },
        "base": { "type": "string", "minLength": 1 },
        "draft": { "type": "boolean" },
        "merged": { "type": "boolean" }
      }
    },
    "gap": {
      "type": "object",
      "additionalProperties": false,
      "required": ["summary", "evidence", "smallest_material_action"],
      "properties": {
        "summary": { "type": "string", "minLength": 1 },
        "evidence": { "$ref": "#/$defs/nonblankStringArray" },
        "smallest_material_action": { "type": "string", "minLength": 1 }
      }
    },
    "claim_ceiling": { "type": "string", "minLength": 1 },
    "status": {
      "type": "object",
      "additionalProperties": false,
      "required": ["candidate", "ready_for_gpt_verification", "accepted", "merged", "current"],
      "properties": {
        "candidate": { "type": "boolean" },
        "ready_for_gpt_verification": { "type": "boolean" },
        "accepted": { "type": "boolean" },
        "merged": { "type": "boolean" },
        "current": { "type": "boolean" }
      }
    },
    "impact_matrix": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["surface", "decision", "reason"],
        "properties": {
          "surface": { "type": "string", "minLength": 1 },
          "decision": { "enum": ["CHANGE", "NO_CHANGE_WITH_REASON", "NOT_APPLICABLE"] },
          "reason": { "type": "string", "minLength": 1 }
        }
      }
    },
    "changed_surfaces": { "$ref": "#/$defs/nonblankStringArray" },
    "explicitly_unaffected_surfaces": { "$ref": "#/$defs/nonblankStringArray" },
    "required_synchronization_decisions": { "$ref": "#/$defs/nonblankStringArray" },
    "schema_tools_tests_workflows_reports_changed": { "$ref": "#/$defs/nonblankStringArray" },
    "validation": {
      "type": "object",
      "additionalProperties": false,
      "required": ["local", "remote"],
      "properties": {
        "local": { "type": "array", "items": { "$ref": "#/$defs/validationItem" } },
        "remote": { "type": "array", "items": { "$ref": "#/$defs/validationItem" } }
      }
    },
    "rollback_strategy": { "type": "string", "minLength": 1 },
    "remaining_limitations": { "$ref": "#/$defs/nonblankStringArray" },
    "receipt_location": { "type": "string", "minLength": 1 }
  },
  "$defs": {
    "sha": { "type": "string", "pattern": "^[0-9a-f]{40}$" },
    "nonblankStringArray": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "string", "minLength": 1, "pattern": "\\S" }
    },
    "validationItem": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "status", "evidence"],
      "properties": {
        "name": { "type": "string", "minLength": 1 },
        "status": { "type": "string", "minLength": 1 },
        "evidence": { "type": "string", "minLength": 1 }
      }
    }
  }
}
