# 121Q32 类型化变更传播与自更新系统图审计

Status: `READY_FOR_GPT_VERIFICATION_CANDIDATE_ONLY`

## Starting truth

- Execution-time `origin/main`: `d1bedb074af8dad8202b4324f3f5bbbb6b308b51`
- Isolated branch: `agent/121q32-typed-change-propagation`
- Draft PR: `#61`
- Current method/map before this candidate: iteration method `1.1.0`, interactive system map `0.1.0`
- Candidate versions: iteration method `1.2.0`, system map `0.2.0`
- Frozen Q29R SHA-256: `c135acd35a2232f0a6b3f933db482932a9fe5d5add51f870af97901faac90d4b`

## Gap and smallest state change

The synchronization registry derives which human, AI, Agent, machine and deployment surfaces require assessment. It explicitly does not model all project entities or substantive relationships. The Current system-map spec also stores identity, canonical target, state and relation content as a second hand-maintained truth. A real project change can therefore leave the map stale without making the existing generator fail.

121Q32 adds the smallest executable bridge: canonical components, typed declared propagation, a deterministic fixpoint, explicit decisions and residue, and a map projection derived from the same registries plus a display-only layout overlay.

## Authority separation

| Domain | Meaning | Propagation authority |
|---|---|---|
| `substantive_causal_candidate` | bounded real/theoretical mechanism candidate | informational only; never automatic |
| `repository_dependency` | declared generation, validation, documentation, publication or deployment relation | automatic or required assessment as declared |
| `synchronization_obligation` | project-governance duty to assess a surface/component | required assessment, not a truth claim |

No Git diff, path match, dependency, traversal, visual edge or zero-residue closure is reported as scientific causal identification.

## Implemented machine chain

`121Q32-request.json → project-components.json + change-propagation-topology.json + synchronization-surfaces.json → compute_change_propagation.py → closure / impact report / map delta / residue → 121Q32 manifest + seal`

The map chain is:

`project-components.json + change-propagation-topology.json + interactive-system-map-layout.json → generate_interactive_system_map.py → materialized interactive-system-map.json + interactive SVG → README / Pages artifact`

The materialized map is now checked against the three upstream authorities. Hidden candidate infrastructure components require a visible representative and an explicit no-change reason. Layout, group order and color remain editorial choices.

## Closure result

- Closure hash: `3d702489aa698747c75b24601530ec73f3c8ced770491e0466f687c02f073414`
- Changed-path seeds: `48`
- Fixpoint: reached in `2` iterations
- Resolved components: `19`
- Typed paths traversed: `15`
- Registry-derived synchronization surfaces: `16`
- Unresolved residue: `0`
- Map delta: 41 nodes and 37 edges gain derived lifecycle/relation-domain metadata; nodes added: 0, nodes removed: 0, edges added: 2 (boundary edges), edges removed: 0; total: 37
- Compatible visible topology: `9 groups / 41 nodes / 37 edges / L0-L6`

Zero residue means every declared mapping and triggered obligation received a decision. It does not prove registry completeness or absence of unknown dependencies.

## Adversarial scenarios

1. A method version change reaches current-state, human/AI/Agent/machine front doors and the map assessment.
2. A new visible component fails unless it enters layout; a hidden component needs a validator-enforced representation and reason.
3. A lifecycle change produces a map delta without converting epistemic candidate status into Current.
4. A Pages-chain change reaches deployment surfaces without rewriting unrelated Foundation documentation.
5. A historical typo can close with `NO_CHANGE_WITH_REASON` and no map delta.
6. Unknown paths and propagation cycles remain blocking residue.
7. Substantive causal candidates cannot use automatic propagation.
8. Historical 1.0.0 and Current 1.1.0 manifests remain valid; only 1.2.0 requires the new binding.

## Lifecycle and external boundary

The Draft Pages workflow builds an inspectable exact-HEAD artifact but does not deploy an unmerged branch to production. `external.pages_homepage` remains pending and blocks Current/Closed, not independent pre-merge review. Local validators always report live external truth as unverified.

Method 1.1.0 and map 0.1.0 remain Current. This candidate adds no L7, truth layer, causal proof, completeness proof or backward rewrite of Q29R and historical manifests.
