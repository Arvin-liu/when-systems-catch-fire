# 任务 102 知识体验层缺口与覆盖审计

状态：候选实现审计；不自证 Accepted、Merged 或 Current。

## 基线缺口

任务 101 的人类层已经提供 README、`HUMAN-READING.md`、`RESULTS/` 和 166 条历史结果记录，但仍存在五个可复现缺口：没有按知识变化组织的时间线；没有按研究问题组织的知识地图；没有统一资产卡；没有长文 1/5/完整三层；没有覆盖旧称、来源、历史、依赖、反向依赖和 supersession 的全量探索入口。

## 候选覆盖

任务 102 用同一份配置和生成器建立 `KNOWLEDGE/` 人类表面与 `data/governance/knowledge-experience/` 机器表面。所有恢复的结果/文章来源都获得卡片与三层阅读；函数和断言 registry 全量进入搜索；任务 98 的重点函数以及公共高风险/结论回弹断言获得详细卡片。

覆盖计数以 `coverage.json` 为准，生成文件摘要与配对以 `manifest.json` 为准。正文中的数字只用于解释候选范围，不替代 exact-head 重算。

## 审计项

- 第一次读者路径和 README 两步可达；
- What's New 的来源、稳定锚点和非 commit 时间线；
- 主题地图的实际资产覆盖；
- 卡片字段、来源摘要、M/E 与 claim ceiling；
- 1 分钟、5 分钟和完整来源三层；
- 全量函数/断言搜索覆盖；
- 别名、撤回与 supersession 防回弹；
- 依赖和反向依赖可见；
- 断链、断锚、默认折叠、孤儿卡片与无来源摘要；
- 人类 Markdown 页面小于 500 KB，详细卡片与全量索引按确定性分片可渲染；
- GitHub Pages 当前面残留；
- clean clone 独立重算。
- canonical 发现器是否排除 `KNOWLEDGE/`、`data/governance/`、任务 99/100 注册表、迁移汇总与任务 101 自动生成的结果投影，同时保留显式 canonical 输入导入，阻断下游输出重新进入上游注册表的反馈环。

## 限制

主题归类、摘要恢复和重要性筛选是确定性仓库规则，不是学科专家裁决。machine-only 表示未生成重点长卡，不表示资产被删除、无价值或已被否定。依赖图只表示仓库声明关系，不是现实因果。候选通过本地或 CI 测试也不证明内容真值。
