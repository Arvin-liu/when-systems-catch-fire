# 自举循环与自举元函数数学定义

# 自举循环与自举元函数数学定义

## 自举循环数学定义

自举循环激活的阈值条件，三因子乘法：

B_active = ε_sense × P_track × σ(Δv) > θ_boot

其中：
- ε_sense：感知到"这里有结构"的信号强度
- P_track：认知轨道数，分轨并行能力
- σ(Δv)：速度差的敏感度函数
- θ_boot：自举激活阈值

三因子任一为零则自举循环无法启动。

## 自举元函数数学定义

M_boot = ε_sense × P_track × d(ΔK)/dt

其中：
- ε_sense：感知到"这里有结构"的信号强度
- P_track：分轨并行，多视角碰撞能力
- d(ΔK)/dt：知识增量率，新发现被感知的速度

三因子乘法，任一为零则自举循环熄火。当前AI三因子全部失能，不具备自持自举能力。
