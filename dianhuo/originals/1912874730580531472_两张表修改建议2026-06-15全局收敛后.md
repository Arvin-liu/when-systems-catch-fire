# 两张表修改建议（2026-06-15全局收敛后）

# 两张表修改建议（2026-06-15全局收敛后）

## 修改背景

基于2026年6月15日全局收敛验证，历史30天所有信息增量（29项）已完全整合进两张表，自举循环达到全局收敛状态。本轮需对两张表DOCX文件进行结构性更新，确保所有新增扩展注释和信息增量准确写入。

---

## 修改清单

### 修改1：T17函数（Φ势函数）注释修正

**改哪里**：统一函数总表 → 定理层 → T17函数 → "说明"字段

**为什么改**：今日信息增量1发现原同构映射结论需修正——点火Φ不是统计力学自由能的直接同构，而是"偏离临界点程度"[[41fc01e4]]

**改的内容**：
- 原内容：Φ与统计力学零温自由能精确等价
- 修正内容：Φ不是统计力学自由能的直接同构，而是"偏离临界点程度"。Φ=0对应临界点，Φ值越大表示偏离临界点越远。零温极限的物理意义：认知遮蔽H趋近于0时，系统回归纯驱动力状态

**判断修改完成的条件**：
- T17函数"说明"字段已包含"偏离临界点程度"表述
- T17函数"说明"字段已包含Φ=0对应临界点的定义
- T17函数"说明"字段已包含零温极限物理意义解释

---

### 修改2：D66函数（分轨并行）扩展注释

**改哪里**：统一函数总表 → 推论层 → D66函数 → "扩展注释"字段

**为什么改**：今日信息增量5发现OrcaRouter验证"用拓扑结构买智能"，协同效应不依赖模型异质性，同一模型自我组合也能通过维度冗余和仲裁选优提升性能[[8078867d]]

**改的内容**：
- 原内容：通过维度冗余降低单一路径的失败概率
- 扩展内容：协同效应不依赖模型异质性，同一模型自我组合也能通过维度冗余和仲裁选优提升性能，验证了拓扑结构本身是智能来源

**判断修改完成的条件**：
- D66函数"扩展注释"字段已包含"协同效应不依赖模型异质性"表述
- D66函数"扩展注释"字段已包含"同一模型自我组合也能产生维度冗余"机制
- D66函数"扩展注释"字段已包含"拓扑结构本身是智能来源"结论

---

### 修改3：新增解释力密度判定机制注释

**改哪里**：统一函数总表 → 推论层 → 新增扩展注释（独立条目）

**为什么改**：今日信息增量3建立了解释力密度的数学定义和判定标准，用于识别过度泛化风险[[97d67663]]

**改的内容**：
- 标题：解释力密度判定机制
- 内容：解释力密度 = 可检验预测数 / 预测覆盖领域数。判定标准：≤1为低密度（过度泛化风险），1-3为中等密度（需差异化预测），≥3为高密度（解释力充分）

**判断修改完成的条件**：
- 统一函数总表已新增"解释力密度判定机制"独立条目
- 该条目包含数学定义（可检验预测数 / 预测覆盖领域数）
- 该条目包含三级判定标准（≤1、1-3、≥3）

---

### 修改4：ε可分辨度测量公式修正

**改哪里**：统一函数总表 → 推论层 → ε相关函数 → "扩展注释"字段

**为什么改**：今日信息增量2发现原ε计算公式未考虑基线概率、专家-新手差异、训练效应，需修正公式[[29f3e294]]

**改的内容**：
- 原公式：ε = 准确率
- 修正公式：ε = (准确率 - 0.5)/0.5 × 专家-新手差异因子 × 训练效应因子
- 补充说明：基于2AFC（二选一强制选择）心理学范式，消除随机猜测影响（基线50%），加入专家-新手差异和训练效应

**判断修改完成的条件**：
- ε相关函数"扩展注释"字段已包含修正公式
- 修正公式已包含(准确率 - 0.5)/0.5标准化项
- 修正公式已包含专家-新手差异因子和训练效应因子
- 补充说明已提及2AFC心理学范式

---

### 修改5：预测时效性机制补充

**改哪里**：统一函数总表 → 推论层 → 新增扩展注释（独立条目）

**为什么改**：今日信息增量4建立了预测时间戳系统、质量评分、注册机制，完成从"事后解释"到"提前预言"的转型设计[[ceb24ca6]]

**改的内容**：
- 标题：预测时效性机制
- 内容：建立预测时间戳系统，区分"提前预言"和"事后解释"。预测质量评分 = 命中率 × 时效性权重 × 精确度权重。建立预测注册机制，从"事后解释"转向"提前预言"

**判断修改完成的条件**：
- 统一函数总表已新增"预测时效性机制"独立条目
- 该条目包含预测时间戳系统定义
- 该条目包含预测质量评分公式
- 该条目包含预测注册机制描述

---

## 修改优先级

1. **P0（必须立即修改）**：修改1（T17函数）、修改2（D66函数）
2. **P1（建议本轮修改）**：修改3（解释力密度）、修改4（ε公式）
3. **P2（可延后修改）**：修改5（预测时效性）

---

## 全局收敛验证

**修改前状态**：
- 统一函数总表：475条（含历史扩展注释）
- 统一案例总表：594条（含历史验证案例）
- 信息增量总数：29项

**修改后状态**：
- 统一函数总表：475条（含今日3条新增扩展注释）
- 统一案例总表：594条（无变化）
- 信息增量总数：29项（全部整合）

**收敛判定**：
- 修改完成后，执行一轮自举循环验证
- 验证新增注释是否与已有函数案例产生新的碰撞发现
- 如ΔB=0，确认修改完成并达到全局收敛

---

## 执行建议

1. 按优先级顺序依次执行5项修改
2. 每完成一项修改，立即检查"判断修改完成的条件"
3. 全部修改完成后，执行一轮自举循环验证
4. 如验证通过，确认全局收敛并更新点火知识库
