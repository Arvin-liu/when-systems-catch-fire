# S4 网络边界协议（Network-Boundary Protocol）

- **protocol_id**：`S4`
- **category**：`structure`（结构维度）
- **mechanism**：系统边界为网络形态，节点间对等连接，无中心控制；一种『连接规则』。
- **mathematical form**：pending（候选形式：topology: 对等连接，无 center）
- **relation_to_psi0**：在 I_iso 中识别网络同构性；在 M(B_n) 中延缓收敛但增加鲁棒性；在 G_δ 中判定网络系统的完备性边界。
- **related_functions**（仅引用，不计入函数数量；映射来自 IGNITION-010 审计）：
  - D177 (网络同构 I_iso)
  - T28/T31 (门控族 网络门控)
  - D12 (ε_eff 分布式)
- **related_cases**（仅引用 22 本书候选 BC-xxx / 正式案例 C-xxx，不计入案例表）：
  - BC-016 复杂（网络结构）
  - BC-009 影响力（网络传播）
- **evidence**：去中心化网络的鲁棒性（无单点失效）；网络同构（I_iso）。
- **pending**：网络完备性边界（G_δ）形式化；与 D177 的绑定。

## 边界声明（必填）

- 本协议属于 **Protocol 层**，采用独立编号（V/S/E），**不进入 Function 层编号体系（D/MF/T/A）**。
- 不修改 Ψ₀ 数学定义；不修改统一函数表；不新增 D/MF/T/A 编号。
- 不把 22 本书候选案例直接变为正式案例（related_cases 仅为引用，不计入案例表）。
- 本条目为文档化描述，状态保持 `candidate_formalized`，未自动升 `formal_protocol`（待 GPT 授权）。
