# S1 封闭边界协议（Closed-Boundary Protocol）

- **protocol_id**：`S1`
- **category**：`structure`（结构维度）
- **mechanism**：系统边界严格封闭，拒绝外部元素输入和内部元素输出；一种『拒绝规则』。
- **mathematical form**：pending（候选形式：boundary: 当 H(外部)→1 时拒绝输入）
- **relation_to_psi0**：在 I_iso 中识别封闭系统内部同构；在 G_δ 中判定封闭系统必然不完备；在 M(B_n) 中加速收敛但限制发现。
- **related_functions**（仅引用，不计入函数数量；映射来自 IGNITION-010 审计）：
  - A6 (H 遮蔽函数 双源)
  - D66 (同质性遮蔽 第三源)
  - D177 (同构 I_iso)
- **related_cases**（仅引用 22 本书候选 BC-xxx / 正式案例 C-xxx，不计入案例表）：
  - BC-013 乌合之众（封闭群体）
  - BC-010 人类简史（边界刚性）
- **evidence**：封闭王朝周期律（内部同构、最终崩溃）；封闭社群的信息茧房（D66 同质性遮蔽）。
- **pending**：封闭→不完备（G_δ）的定量形式；与 A6/D66 的边界映射。

## 边界声明（必填）

- 本协议属于 **Protocol 层**，采用独立编号（V/S/E），**不进入 Function 层编号体系（D/MF/T/A）**。
- 不修改 Ψ₀ 数学定义；不修改统一函数表；不新增 D/MF/T/A 编号。
- 不把 22 本书候选案例直接变为正式案例（related_cases 仅为引用，不计入案例表）。
- 本条目为文档化描述，状态保持 `candidate_formalized`，未自动升 `formal_protocol`（待 GPT 授权）。
