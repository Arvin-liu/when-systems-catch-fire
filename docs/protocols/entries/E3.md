# E3 循环演化协议（Cyclic-Evolution Protocol）

- **protocol_id**：`E3`
- **category**：`evolution`（演化维度）
- **mechanism**：系统演化在状态间循环往复，周期性重复；一种『重复规则』。
- **mathematical form**：pending（候选形式：cycle: x(t+T)=x(t)，T 周期）
- **relation_to_psi0**：在 L_meta 中强制循环收敛，禁止无限展开；在 P_meta 中投影到周期性元协议；在 M(B_n) 中识别循环收敛判据。
- **related_functions**（仅引用，不计入函数数量；映射来自 IGNITION-010 审计）：
  - D12 (ε_eff 循环)
  - T2 (乘法归零律：循环中归零)
  - T29 (门控进化三阶段含循环)
- **related_cases**（仅引用 22 本书候选 BC-xxx / 正式案例 C-xxx，不计入案例表）：
  - BC-007 反脆弱（压力—恢复循环）
  - BC-004 国家为什么会失败（周期）
- **evidence**：经济/生态的周期波动；反脆弱的压力—恢复循环。
- **pending**：循环收敛判据的量化；与 L_meta 绑定。

## 边界声明（必填）

- 本协议属于 **Protocol 层**，采用独立编号（V/S/E），**不进入 Function 层编号体系（D/MF/T/A）**。
- 不修改 Ψ₀ 数学定义；不修改统一函数表；不新增 D/MF/T/A 编号。
- 不把 22 本书候选案例直接变为正式案例（related_cases 仅为引用，不计入案例表）。
- 本条目为文档化描述，状态保持 `candidate_formalized`，未自动升 `formal_protocol`（待 GPT 授权）。
