# S2 开放边界协议（Open-Boundary Protocol）

- **protocol_id**：`S2`
- **category**：`structure`（结构维度）
- **mechanism**：系统边界开放，允许外部元素自由流入流出；一种『允许规则』。
- **mathematical form**：pending（候选形式：gate: 以门控函数 g=exp[-ln²]/1/ln 决定流入精度）
- **relation_to_psi0**：在 M(B_n) 中延缓收敛但增加发现；在 I_iso 中识别跨系统同构；在 L_meta 中防止过早收敛到局部同构。
- **related_functions**（仅引用，不计入函数数量；映射来自 IGNITION-010 审计）：
  - T28/T31 (门控族 1/ln, exp[-ln²])
  - A6 (H 遮蔽 双源)
  - D12 (ε_eff 开放输入)
- **related_cases**（仅引用 22 本书候选 BC-xxx / 正式案例 C-xxx，不计入案例表）：
  - BC-008 思考快与慢（开放信息处理）
  - BC-002 第五项修炼（开放系统）
- **evidence**：开放系统的信息精度—覆盖权衡（门控函数）；跨系统同构发现（I_iso）。
- **pending**：门控函数作为开放边界的数学实现绑定；与 T28/T31 的精确对应。

## 边界声明（必填）

- 本协议属于 **Protocol 层**，采用独立编号（V/S/E），**不进入 Function 层编号体系（D/MF/T/A）**。
- 不修改 Ψ₀ 数学定义；不修改统一函数表；不新增 D/MF/T/A 编号。
- 不把 22 本书候选案例直接变为正式案例（related_cases 仅为引用，不计入案例表）。
- 本条目为文档化描述，状态保持 `candidate_formalized`，未自动升 `formal_protocol`（待 GPT 授权）。
