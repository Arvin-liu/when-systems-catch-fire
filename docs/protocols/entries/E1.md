# E1 线性演化协议（Linear-Evolution Protocol）

- **protocol_id**：`E1`
- **category**：`evolution`（演化维度）
- **mechanism**：系统演化沿单一轨迹线性推进，状态变化可预测；一种『路径约束』。
- **mathematical form**：pending（候选形式：path: x(t+1)=f(x(t))，f 线性）
- **relation_to_psi0**：在 C(x,y) 中识别线性因果链；在 M(B_n) 中加速收敛但限制发现；在 G_δ 中判定线性系统完备性边界。
- **related_functions**（仅引用，不计入函数数量；映射来自 IGNITION-010 审计）：
  - T2 (乘法归零律：线性链归零)
  - D12 (ε_eff 线性递推)
- **related_cases**（仅引用 22 本书候选 BC-xxx / 正式案例 C-xxx，不计入案例表）：
  - BC-019 规模（线性 scaling）
  - BC-011 未来简史（线性叙事）
- **evidence**：线性 scaling 的局限（规模律在极端失效）；线性因果链的可预测性。
- **pending**：线性陷阱的定量判据；与 G_δ 的边界。

## 边界声明（必填）

- 本协议属于 **Protocol 层**，采用独立编号（V/S/E），**不进入 Function 层编号体系（D/MF/T/A）**。
- 不修改 Ψ₀ 数学定义；不修改统一函数表；不新增 D/MF/T/A 编号。
- 不把 22 本书候选案例直接变为正式案例（related_cases 仅为引用，不计入案例表）。
- 本条目为文档化描述，状态保持 `candidate_formalized`，未自动升 `formal_protocol`（待 GPT 授权）。
