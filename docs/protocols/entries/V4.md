# V4 可持续性协议（Sustainability Protocol）

- **protocol_id**：`V4`
- **category**：`value`（价值维度）
- **mechanism**：系统以长期动态平衡为目标，在扩张与收缩间维持循环；要求主动接受『收缩』。
- **mathematical form**：pending（候选形式：循环收敛判据于 L_meta，禁止单向演化）
- **relation_to_psi0**：在 L_meta 中强制循环收敛，禁止单向演化；在 P_meta 中投影到周期性元协议；在 M(B_n) 中识别循环收敛判据。
- **related_functions**（仅引用，不计入函数数量；映射来自 IGNITION-010 审计）：
  - D12 (ε_eff 闭环/循环)
  - T2 (乘法归零律：循环中的归零风险)
- **related_cases**（仅引用 22 本书候选 BC-xxx / 正式案例 C-xxx，不计入案例表）：
  - BC-004 国家为什么会失败（长期平衡）
  - BC-014 娱乐至死（过度扩张警示）
- **evidence**：生态/经济系统的扩张—收缩周期；门控函数的循环面（T29 三阶段）。
- **pending**：循环收敛的定量判据；与 L_meta 的绑定。

## 边界声明（必填）

- 本协议属于 **Protocol 层**，采用独立编号（V/S/E），**不进入 Function 层编号体系（D/MF/T/A）**。
- 不修改 Ψ₀ 数学定义；不修改统一函数表；不新增 D/MF/T/A 编号。
- 不把 22 本书候选案例直接变为正式案例（related_cases 仅为引用，不计入案例表）。
- 本条目为文档化描述，状态保持 `candidate_formalized`，未自动升 `formal_protocol`（待 GPT 授权）。
