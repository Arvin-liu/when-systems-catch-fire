# V2 效率性协议（Efficiency Protocol）

- **protocol_id**：`V2`
- **category**：`value`（价值维度）
- **mechanism**：系统以资源利用效率最大化为目标的演化规则，优化输入产出比；会关闭『低效但必要』的选项。
- **mathematical form**：pending（候选形式：效率瓶颈识别于 C(x,y) 因果链的 throughput/cycle-time）
- **relation_to_psi0**：在 C(x,y) 因果结构判定中识别因果链效率瓶颈；在 M(B_n) 中提供收敛加速（可能错过全局最优）；在 G_δ 中识别效率悖论。
- **related_functions**（仅引用，不计入函数数量；映射来自 IGNITION-010 审计）：
  - D12 (ε_eff 效率/闭环)
  - T20 (σ_opt=√e 最优权衡)
  - T28/T31 (门控族：精度—覆盖权衡)
- **related_cases**（仅引用 22 本书候选 BC-xxx / 正式案例 C-xxx，不计入案例表）：
  - BC-002 第五项修炼（系统效率）
  - BC-019 规模（规模效率）
- **evidence**：组织为局部效率牺牲鲁棒性（效率陷阱）；门控函数 σ_opt 给出精度—覆盖的最优折中。
- **pending**：效率悖论的形式化（G_δ 侧）；与 T20 的解析绑定。

## 边界声明（必填）

- 本协议属于 **Protocol 层**，采用独立编号（V/S/E），**不进入 Function 层编号体系（D/MF/T/A）**。
- 不修改 Ψ₀ 数学定义；不修改统一函数表；不新增 D/MF/T/A 编号。
- 不把 22 本书候选案例直接变为正式案例（related_cases 仅为引用，不计入案例表）。
- 本条目为文档化描述，状态保持 `candidate_formalized`，未自动升 `formal_protocol`（待 GPT 授权）。
