# E2 非线性演化协议（Non-linear-Evolution Protocol）

- **protocol_id**：`E2`
- **category**：`evolution`（演化维度）
- **mechanism**：系统演化路径非线性，小变化可能引发大结果；一种『敏感度规则』。
- **mathematical form**：pending（候选形式：sensitivity: ∂x/∂ε 大，门控区 Fisher 距离 Ξ=∞）
- **relation_to_psi0**：在 C(x,y) 中识别非线性因果链；在 M(B_n) 中延缓收敛但增加发现；在 G_δ 中判定非线性系统完备性边界。
- **related_functions**（仅引用，不计入函数数量；映射来自 IGNITION-010 审计）：
  - T28 (高斯门控 exp[-ln²])
  - D120 (不可逆性判据 Ξ(G))
  - T31 (门控信息熵跃迁)
- **related_cases**（仅引用 22 本书候选 BC-xxx / 正式案例 C-xxx，不计入案例表）：
  - BC-006 黑天鹅（非线性风险）
  - BC-022 混沌（敏感依赖）
- **evidence**：黑天鹅事件（小因大果）；门控区的不可逆性（D120 Ξ=∞）。
- **pending**：非线性→不可逆（D120）的绑定；与 G_δ 边界。

## 边界声明（必填）

- 本协议属于 **Protocol 层**，采用独立编号（V/S/E），**不进入 Function 层编号体系（D/MF/T/A）**。
- 不修改 Ψ₀ 数学定义；不修改统一函数表；不新增 D/MF/T/A 编号。
- 不把 22 本书候选案例直接变为正式案例（related_cases 仅为引用，不计入案例表）。
- 本条目为文档化描述，状态保持 `candidate_formalized`，未自动升 `formal_protocol`（待 GPT 授权）。
