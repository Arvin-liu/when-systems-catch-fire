# S3 层级边界协议（Hierarchical-Boundary Protocol）

- **protocol_id**：`S3`
- **category**：`structure`（结构维度）
- **mechanism**：系统边界按层级划分，高层级可访问低层级，反之不行；一种『方向性规则』。
- **mathematical form**：pending（候选形式：access: level(high) → level(low) 单向）
- **relation_to_psi0**：在 L_meta 中强制层级收敛，禁止跨层级无限展开；在 P_meta 中投影到层级元协议；在 C(x,y) 中识别因果链层级断裂。
- **related_functions**（仅引用，不计入函数数量；映射来自 IGNITION-010 审计）：
  - T2 (乘法归零律：层级链任一断点归零)
  - A9 (P_exit 层级退出权)
  - A6 (H 层级遮蔽)
- **related_cases**（仅引用 22 本书候选 BC-xxx / 正式案例 C-xxx，不计入案例表）：
  - BC-012 文明的冲突（层级结构）
  - BC-003 枪炮病菌与钢铁（层级权力）
- **evidence**：科层制的单向信息流；层级断裂导致系统失效（T2 语义）。
- **pending**：层级收敛的定量判据；与 L_meta 的绑定。

## 边界声明（必填）

- 本协议属于 **Protocol 层**，采用独立编号（V/S/E），**不进入 Function 层编号体系（D/MF/T/A）**。
- 不修改 Ψ₀ 数学定义；不修改统一函数表；不新增 D/MF/T/A 编号。
- 不把 22 本书候选案例直接变为正式案例（related_cases 仅为引用，不计入案例表）。
- 本条目为文档化描述，状态保持 `candidate_formalized`，未自动升 `formal_protocol`（待 GPT 授权）。
