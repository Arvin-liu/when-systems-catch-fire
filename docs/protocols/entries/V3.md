# V3 创新性协议（Innovation Protocol）

- **protocol_id**：`V3`
- **category**：`value`（价值维度）
- **mechanism**：系统选择使复杂度（熵）增加最大的行动，追求可能性空间扩张；主动选择不确定性高的路径。
- **mathematical form**：pending（候选形式：选择 argmax_ΔS 路径，S 为复杂度/熵）
- **relation_to_psi0**：在 M(B_n) 中防止收敛过早、保持探索性；在 G_δ 中触发『必须往元层面收敛』判定；在 I_iso 中识别创新与已有结构的同构性。
- **related_functions**（仅引用，不计入函数数量；映射来自 IGNITION-010 审计）：
  - D177 (深层同构 I_iso)
  - T42 (G_δ 不完备→必须元层面收敛)
  - T31 (门控信息熵跃迁 1/ln)
- **related_cases**（仅引用 22 本书候选 BC-xxx / 正式案例 C-xxx，不计入案例表）：
  - BC-005 创新者的窘境（创新边界）
  - BC-016 复杂（复杂度扩张）
- **evidence**：系统在自我强化中封闭（创新停滞）；门控信息熵跃迁 1/ln 给出探索—利用的熵临界。
- **pending**：与 I_iso 的『创新即新同构』判据；与 G_δ 的触发条件对齐。

## 边界声明（必填）

- 本协议属于 **Protocol 层**，采用独立编号（V/S/E），**不进入 Function 层编号体系（D/MF/T/A）**。
- 不修改 Ψ₀ 数学定义；不修改统一函数表；不新增 D/MF/T/A 编号。
- 不把 22 本书候选案例直接变为正式案例（related_cases 仅为引用，不计入案例表）。
- 本条目为文档化描述，状态保持 `candidate_formalized`，未自动升 `formal_protocol`（待 GPT 授权）。
