# 生命完整性未来激活边界 R5-A R1（Life Integrity Future Activation Boundary R1）

> 候选。明确 R5-A 之后的迭代门槛与未授权范围。R5-A 自身不激活任何候选。

## 1. 本轮显式未授权（硬禁止）

- R5-B 具身生命系统运行时（Embodied Life System runtime）；
- R5-C《新悟真篇》现代悟真领域包（Modern Wuzhen Domain Pack）；
- 宗教源文本摄入或新宗教语料；
- 对真实人的实践指令；
- 医疗/精神科/诊断/治疗主张；
- 人类健康数据收集；
- 人类画像数据库；
- 真实世界实验；
- 领域包联邦实现；
- L7 或第二执行器；
- PROMOTE / EVOLVE / Ready / merge / Main 修改 / 强推 / 历史重写 / 私有或重构内容公开发布。

## 2. 候选激活计数器

本候选所有激活相关计数器保持为零（`modern_wuzhen_pack_started=false`、`domain_pack_federation_started=false`、`human_intervention_enabled=false`、`external_acceptance_claimed=false`）。任何激活必须经用户显式授权与独立外部审查，不得在 R5-A 内自启动。

## 3. 后续迭代门槛（参考 backlog）

未来任务（R5-B/C/R6）只有在满足以下条件后才可被 GPT 转为正式任务：

- R3 已达到 Draft 待外部审查终态且真实指标/失败完整可读；
- R4 已区分运行/表示/来源/模型/架构/治理失败；
- 证明新增架构不是对现有 Charter/ARR/Function OS 的重复；
- 明确最小实现范围与 NonImpactProof；
- 用户显式授权。

## 4. 终态

`IGNITION_R5A_LIFE_INTEGRITY_CHARTER_CANDIDATE_DRAFT_AWAITING_EXTERNAL_REVIEW`
