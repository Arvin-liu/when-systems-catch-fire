# 历史函数深审路线图

本轮完成全量发现和首批 12 项纠偏，不宣称已完成 2,033 项资产的数学、领域或经验审定。

后续批次按机器队列恢复：

1. `CRITICAL`：本轮已纠偏资产及其仍未处理的直接消费者；
2. `HIGH`：未定义显式 ID、隐式命名资产、身份冲突和强全称词命中；
3. `STANDARD`：其余已注册对象，按反向依赖数和证据风险排序。

每批必须固定 `resume_key`、formal main 起点、输入清单和输出哈希；完成定义、维度、反例、循环、层级、ceiling、同构、全称、内部测试真值和依赖传播十门。机器不确定项保持 `REQUIRES_HUMAN_REVIEW`。

批次完成的含义仅是该批记录得到相应审定；不会自动把整个函数表、领域理论或点火项目宣布为已证明。
