# 经典问题 benchmark

本文记录点火项目 v0.2 阶段的经典问题 benchmark。

## 当前初稿

- [经典问题 benchmark 初稿：得到大脑输出 20260706](../outputs/getbrain/classic-problems-benchmark-draft-20260706.md)

## 当前补丁

- [经典问题 benchmark 补丁：补齐 2 个条目与计数复核](../outputs/getbrain/classic-problems-benchmark-supplement-20260707.md)

## 定位

经典问题 benchmark 用于测试点火框架在各学科经典问题上的表现。

它不是普通案例库，而是测试集。

普通案例库更像训练材料，用于生成函数、案例和推论；经典问题 benchmark 用于检验点火框架是否真的有结构增益，还是只是重述、失败或 `pending`。

## 当前测试维度

每个经典问题主要检查：

- 所属领域；
- 问题定义；
- 主流解释路径；
- 证据制度；
- 点火框架映射；
- 结构性增益；
- 点火框架只是重述的部分；
- 可能失败类型；
- 当前断言等级；
- benchmark 结果；
- 后续需要的证据或验证；
- 是否适合作为公开案例。

## benchmark 结果类型

当前采用以下结果类型：

1. 结构增益 + `pending`；
2. 结构增益 + 重述；
3. 重述为主；
4. 失败；
5. 待补；
6. 待复核。

## 当前已覆盖领域

初稿已覆盖多个领域，包括：

- 数学；
- 物理学；
- 历史学；
- 经济学；
- 社会科学；
- 政治学；
- 心理学；
- 教育学；
- AI / 计算机科学；
- 文学 / 艺术。

## 已补齐条目

任务 G 原稿中说明，由于输出长度限制，仍有 2 个问题待补：

1. 叙事为什么能改变人的理解；
2. 现代艺术为何成立。

这两个问题已由任务 G 补丁补齐，补丁文件见：

- [classic-problems-benchmark-supplement-20260707.md](../outputs/getbrain/classic-problems-benchmark-supplement-20260707.md)

## 计数复核结果

任务 G 原稿中提到“前 30 个经典问题中的 28 个”，但补丁复核发现：

- 原任务 G 总表实际列出 31 个 benchmark 条目；
- 本次补丁新增 2 个 benchmark 条目；
- 当前 benchmark 条目总数为 33 个。

后续仍需完成：

1. 为每个 benchmark 分配唯一编号；
2. 建议编号格式为 CP-001 至 CP-033；
3. 将 benchmark 总表整理为机器可读 CSV / JSON；
4. 保留原始任务 G 文件作为历史版本，不直接重写其正文。

## 与证据制度库的关系

每个 benchmark 问题都必须调用对应学科的证据制度，判断该问题的成立标准与 `L0-L5` 断言等级。

## 与失败类型学的关系

每个 benchmark 问题都必须标注可能失败类型，例如：

- 材料错误；
- 边界选错；
- 尺度错配；
- 证据等级不足；
- 学科证据制度误配；
- AI 过度解释；
- `L2` 推论误写成 `L5` 结论。

## 与公开案例的关系

如果 benchmark 结果是“结构增益 + `pending`”，可作为结构性推论样本，但不能写成“已解决”。

如果 benchmark 结果是“结构增益 + 重述”，可作为跨域同构识别样本，但应明确哪些部分只是重述。

如果 benchmark 结果是“失败”或高风险未处理，不应直接公开故事化。

## 后续改进

后续需要继续：

1. 为 33 个 benchmark 分配唯一编号，建议使用 CP-001 至 CP-033；
2. 建立机器可读 CSV / JSON；
3. 与故事化案例 backlog 联动；
4. 与失败案例库联动；
5. 与 UNESCO 学科深跑联动；
6. 后续如继续新增 benchmark，应记录为补丁或新版本，不直接覆盖原稿。
