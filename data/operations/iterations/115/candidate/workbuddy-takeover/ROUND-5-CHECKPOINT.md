# Round 5 Checkpoint — Frozen Offline Anti-Overfit / Error-Stop Fixtures

**Round:** 5 / 7
**Parent (frozen Qwen Round 0):** `f4fe6faded65c16c98230ad34ca17e4374d59613`
**Round 5 deliverable commit:** `7ceb4280444396e8e57a20dee7f37105829b728b`
**Branch:** `workbuddy/task115-deep-research-queue-round1-7-takeover-r1-20260804`

## What Round 5 produced

TASK.md Round 5 requires **≥24 frozen offline fixtures** covering the
anti-overfit / error-stop surface, a **deterministic replay + regression test**,
and **separate (uncollapsed) metrics**. All three are delivered. The fixtures are
generated by a canonical generator that self-validates against the live
evaluator/queue runtime, so they can never silently drift into a vacuous state.

### `tools/deep_research/generate_round5_fixtures.py` (NEW, with fixes)
- Canonical generator emitting **16 episode + 11 queue = 27 fixtures** (≥24) to
  `tests/fixtures/deep_research/round5/`.
- Each fixture carries an `expect` block (intended `decision` for episodes;
  intended queue op result). `emit_episode_fixture` recomputes the actual
  evaluator decision and **raises on divergence**; `self_validate_queue` replays
  `recover` / `select_next` (duplicate lease) / `should_stop` / `ingest_result`
  and asserts the behaviour matches `expect`.
- Fixes applied this session: corrected the `sys.path` root (generator lives in
  `tools/deep_research/`, so the repo-root is `parents[2]`); `obs_discovered`
  now supplies the `{"url": ...}` shape the adapter's `search` expects;
  `obs_abstract` builds a genuine `ABSTRACT_ONLY` observation (the clean
  `WebAdapter.open` path only yields `ABSTRACT_ONLY` on provenance
  contamination, which would mis-fire the prompt-injection gate);
  `self_validate_queue` calls the **module-level** `select_next` /
  `should_stop` so the distinct-owner and `low_info_threshold` parameters are
  actually honored.

### `tests/fixtures/deep_research/round5/*.json` (GENERATED, 27)
**Episode scenarios (r5-001..016)** — many URLs without reading; repeated
same-family sources; summary-only claimed as full text; unsupported citation;
absent contrary evidence; conflicting estimands; unavailable data with honest
`TENTATIVE` ceiling reduction; numerical mismatch; high-stakes escalation;
prompt injection; genuinely-sufficient bounded research; unfrozen scope; open
HIGH obligation; single-family material claim; blocked (NONE-access)
evidence route; non-entailing pair behind an open obligation.

**Queue scenarios (r5-017..027)** — crash/resume (expired lease → QUEUED,
checkpoint preserved); duplicate lease (second owner refused); deadline /
max-episodes / budget / owner / safety-blocker / queue-empty / low-information
stops; and the two hard anti-overfit invariants — a **long report with 200 URLs**
and an **executor `success`** result must **NEVER** stop the queue.

### `tests/test_deep_research_round5.py` (NEW) — 4 methods (27 subTests), all PASS
- `test_replay_each_episode_matches_expect` — live `SufficiencyEvaluator`
  reproduces each fixture's frozen `decision` and `failed_gates` exactly.
- `test_replay_each_queue_op_matches_expect` — live `SerialQueue` reproduces each
  queue fixture's `recover` / duplicate-lease refusal / `should_stop` reason /
  ingest-never-stops behaviour.
- `test_separate_metrics_reported_not_collapsed` — computes **separate**
  indicators (no single score): `brief_present`, `obligation_coverage`,
  `source_family_count`, `abstract_only_count`, `none_access_count`,
  `injection_detected`, `contrary_sought`, plus **every** sufficiency-vector
  dimension; writes `ROUND5_METRICS.json`; and computes **false-positive /
  false-negative stop rates against an independent ground-truth label** (not
  against the evaluator's own output).
- `test_total_fixtures_ge_24` — guards the ≥24 floor.

### Separate metrics (from `ROUND5_METRICS.json`)
- **Decision distribution:** CONTINUE_RESEARCH 9, STOP_SUFFICIENT_CANDIDATE 3,
  ESCALATE_GPT_OWNER 3, BLOCKED_WITH_EVIDENCE 1.
- **Episode stop confusion (vs independent GT):** TP 6, FP 1, FN 0, TN 9.
  The single FP is **r5-006 conflicting-estimands** — two contradictory material
  claims asserted is a defect, but the evaluator has no contradiction gate yet,
  so it currently returns `STOP_SUFFICIENT_CANDIDATE`. This is a **known gap**
  surfaced (not hidden) by the metric and tracked for Round 7 Codex handoff.
- **Queue stop confusion (vs independent GT):** TP 7, FP 0, FN 0, TN 2. The
  queue's hard anti-overfit property holds — **zero** false-positive stops from
  long reports / executor success, and **zero** false negatives on the seven
  legitimate campaign stop conditions.

## Test results
- `tests/test_deep_research_round1.py`: **19/19 PASS**
- `tests/test_deep_research_round2.py`: **24/24 PASS**
- `tests/test_deep_research_round3.py`: **18/18 PASS**
- `tests/test_deep_research_round4.py`: **13/13 PASS**
- `tests/test_deep_research_round5.py`: **4/4 methods (27 subTests) PASS**
  → **97/97 across R1–R5, no regression**
- `tools/deep_research/queue_runtime.py` smoke: OK
- `tests/test_research_os.py`: ALL CORE TESTS PASSED
- `tests/test_research_os_checkpoint_c.py`: ALL CHECKPOINT C TESTS PASSED
- `tests/test_research_os_resumability.py`: ALL RESUMABILITY/REPLAY TESTS PASSED

## Next
Round 6 — bounded sleep-timing live pilot (freeze question: late sleep timing /
delayed circadian phase for 7–8h sleepers). Only if the runtime is stable and
the available tools permit; otherwise record `BLOCKED_WITH_EVIDENCE` with the
exact evidence of why the pilot could not run, and carry the limitation forward
to the Round 7 Codex handoff + GPT review packet.
