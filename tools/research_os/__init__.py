"""Pointfire Research Executive OS - WorkBuddy Draft PR candidate (Task 115).

Active cross-layer research controller spanning L0-L6. NOT an L7, NOT a truth
authority, NOT a publisher, NOT a replacement for Function OS / Q12 / Q13 /
Foundation / Charter Gate / language-thought logic. The deterministic core runs
without any LLM; an LLM may only supply optional candidate proposals.

This package is the Checkpoint B executable loop: episode state kernel,
obligation graph, deterministic diagnosis engine, inspectable scheduler, executor
adapter contract and CLI.
"""

__version__ = "0.1.0-draft"
